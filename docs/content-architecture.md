# Архитектура контента PromptHub

## Обзор

Данный документ описывает архитектуру контентной части проекта PromptHub, включая организацию промптов, тегов, категорий и статей.

---

## Структура данных

### 1. Промпты (Prompts)

**Модель:** `Prompt` в `prisma/schema.prisma`

**URL-паттерн:**
- Страница промпта: `/{locale}/prompt/{id}`
- Список промптов: `/{locale}/prompts`

**Основные поля:**
- `id` - уникальный идентификатор
- `title` - заголовок промпта
- `description` - краткое описание
- `prompt` - текст промпта
- `model` - модель AI (ChatGPT, Claude, Gemini и т.д.)
- `lang` - язык промпта
- `category` - категория (legacy string, постепенно заменяется на categoryId)
- `categoryId` - связь с новой моделью Category
- `tags` - теги (legacy string, постепенно заменяется на PromptTag)
- `license` - лицензия использования
- `authorId` - автор промпта
- `averageRating` - средняя оценка
- `totalRatings` - количество оценок
- `views` - количество просмотров

**Связи:**
- `author` → User
- `categoryRef` → Category
- `promptTags` → PromptTag (многие-ко-многим с Tag)
- `ratings`, `reviews`, `likes`, `saves`, `comments` - пользовательские взаимодействия

---

### 2. Категории (Categories)

**Модель:** `Category` в `prisma/schema.prisma`

**URL-паттерн:**
- Страница категории: `/{locale}/category/{slug}`

**Основные поля:**
- `id` - уникальный идентификатор
- `slug` - URL-слаг (уникальный)
- `nameRu` - название на русском
- `nameEn` - название на английском
- `descriptionRu` - описание на русском
- `descriptionEn` - описание на английском
- `parentId` - ID родительской категории (для иерархии)
- `isActive` - активна ли категория
- `sortOrder` - порядок сортировки
- `promptCount` - кэш количества промптов (обновляется автоматически)

**Особенности:**
- Поддержка иерархии (родитель-дочерние)
- Полная i18n поддержка (русский и английский)
- Автоматический подсчет промптов через связи

**Утилиты:**
- `lib/categories.ts` - функции для работы с категориями
- `lib/seo.ts` - генерация SEO метаданных

---

### 3. Теги (Tags)

**Модель:** `Tag` в `prisma/schema.prisma`

**URL-паттерн:**
- Страница тега: `/{locale}/tag/{slug}`

**Основные поля:**
- `id` - уникальный идентификатор
- `slug` - URL-слаг (уникальный)
- `name` - название тега
- `description` - описание
- `isNsfw` - NSFW тег (для 18+ контента)
- `isActive` - активен ли тег
- `color` - цвет тега (hex, опционально)
- `promptCount` - кэш количества промптов

**Связи:**
- `promptTags` → PromptTag (многие-ко-многим с Prompt)
- `articleTags` → ArticleTag (многие-ко-многим с Article)

**Особенности:**
- Теги используются как для промптов, так и для статей
- Поддержка NSFW-фильтрации
- Кастомные цвета для визуальной идентификации

---

### 4. Статьи (Articles)

**Модель:** `Article` в `prisma/schema.prisma`

**URL-паттерн:**
- Список статей: `/{locale}/articles`
- Страница статьи: `/{locale}/articles/{slug}`

**Основные поля:**
- `id` - уникальный идентификатор
- `slug` - URL-слаг (уникальный)
- `titleRu`, `titleEn` - заголовок на русском и английском
- `descriptionRu`, `descriptionEn` - описание на русском и английском
- `contentRu`, `contentEn` - полный контент в формате Markdown
- `coverImage` - URL изображения обложки (опционально)
- `authorId` - автор статьи
- `status` - статус: `draft`, `published`, `archived`
- `publishedAt` - дата публикации
- `viewCount` - счетчик просмотров

**Связи:**
- `author` → User
- `articleTags` → ArticleTag (многие-ко-многим с Tag)

**Особенности:**
- Полная i18n поддержка (русский и английский)
- Markdown контент с поддержкой GFM (GitHub Flavored Markdown)
- SEO-оптимизация (мета-теги, JSON-LD, Open Graph)
- Автоматическая генерация оглавления из H2/H3
- Система черновиков и публикации

**Компоненты:**
- `components/articles/ArticleLayout.tsx` - главный layout статьи
- `components/articles/ArticleContent.tsx` - рендеринг Markdown
- `components/articles/ArticleTags.tsx` - отображение тегов
- `components/articles/ArticleAuthor.tsx` - информация об авторе
- `components/articles/ArticleTableOfContents.tsx` - оглавление
- `components/articles/RelatedArticles.tsx` - связанные статьи
- `components/articles/RelatedPrompts.tsx` - связанные промпты
- `components/articles/ArticleCTA.tsx` - призыв к действию
- `components/articles/ArticleCard.tsx` - карточка статьи для списка
- `components/articles/RelatedArticlesWidget.tsx` - виджет для страниц промптов

**Репозиторий:**
- `lib/repositories/articleRepository.ts` - все операции с статьями:
  - `listArticles()` - список статей с пагинацией
  - `getArticleBySlug()` - получение статьи по slug
  - `getRelatedArticles()` - связанные статьи по тегам
  - `getArticlesByTags()` - статьи для указанных тегов
  - `incrementViewCount()` - увеличение счетчика просмотров
  - `getRecentArticles()` - последние опубликованные статьи

**SEO утилиты:**
- `lib/seo-articles.ts` - генерация метаданных:
  - `generateArticlesListMetadata()` - для списка статей
  - `generateArticleMetadata()` - для страницы статьи
  - `generateArticleStructuredData()` - JSON-LD для Article
  - `generateArticleBreadcrumbStructuredData()` - хлебные крошки

---

## Маршруты и страницы

### Публичные маршруты

```
app/[locale]/(public)/
├── home/page.tsx                    # Главная страница
├── prompts/page.tsx                 # Список промптов
├── prompt/[id]/page.tsx            # Страница промпта
├── category/[slug]/page.tsx        # Страница категории
├── tag/[slug]/page.tsx             # Страница тега
├── articles/                        # 🆕 Раздел статей
│   ├── page.tsx                    # Список статей
│   └── [slug]/page.tsx             # Страница статьи
├── leaders/page.tsx                # Таблица лидеров
└── add/page.tsx                    # Добавление промпта
```

### API маршруты

```
app/api/
├── prompts/                        # CRUD промптов
│   ├── route.ts
│   └── [id]/
│       ├── route.ts
│       ├── similar/route.ts
│       └── views/route.ts
├── categories/route.ts             # Работа с категориями
├── tags/route.ts                   # Работа с тегами
├── articles/                        # 🆕 API статей
│   └── by-tags/route.ts            # Получение статей по тегам
├── ratings/route.ts                # Рейтинги
├── reviews/route.ts                # Отзывы
└── search-suggestions/route.ts     # Подсказки поиска
```

---

## Sitemap структура

```
sitemap.xml (индекс)
├── sitemaps/root.xml               # Главная, основные страницы
├── sitemaps/categories.xml         # Все категории
├── sitemaps/tags.xml               # Все теги
├── sitemaps/articles.xml           # 🆕 Все статьи
├── sitemaps/ru.xml                 # Локализованные страницы (RU)
├── sitemaps/en.xml                 # Локализованные страницы (EN)
└── sitemaps/prompts-[page].xml    # Промпты (пагинированные)
```

**Приоритеты:**
- Главная: 1.0
- Список промптов/статей: 0.8
- Отдельный промпт: 0.8
- Категория: 0.7
- Статья: 0.7
- Тег: 0.6

**Частота обновления:**
- Главная: daily
- Категории/теги: weekly
- Статьи/промпты: monthly

---

## Интеграция и перелинковка

### Перелинковка контента

1. **Промпты → Статьи**
   - На странице промпта отображается виджет `RelatedArticlesWidget`
   - Связь по общим тегам
   - Автоматическая загрузка через API `/api/articles/by-tags`

2. **Статьи → Промпты**
   - В статье блок `RelatedPrompts` показывает подходящие промпты
   - Связь по тегам из статьи
   - Прямые ссылки на страницы промптов и тегов

3. **Теги → Контент**
   - Страницы тегов показывают промпты
   - В будущем можно добавить статьи на страницы тегов

4. **Категории → Статьи**
   - Планируется: связь статей с категориями
   - Для этого нужно добавить поле `categoryId` в модель Article

---

## Локализация (i18n)

**Поддерживаемые языки:**
- Русский (`ru`) - основной
- Английский (`en`)

**Механизм:**
- `next-intl` для переводов интерфейса
- Дублирование контента в полях `*Ru` и `*En` для SEO
- URL с префиксом локали: `/ru/...`, `/en/...`
- Автоопределение языка по cookies и Accept-Language

**Переводы:**
- Интерфейс: `messages/ru.json`, `messages/en.json`
- Контент (категории, статьи): поля в БД

---

## SEO оптимизация

### Метаданные

Каждая страница имеет:
- `title` - уникальный заголовок
- `description` - уникальное описание
- `keywords` - ключевые слова
- `canonical` - канонический URL
- `alternates` - hreflang для языков
- Open Graph теги (og:title, og:description, og:image)
- Twitter Card теги

### Структурированные данные (JSON-LD)

- **Article** - для статей
- **BreadcrumbList** - хлебные крошки
- **CollectionPage** - для категорий
- Планируется: **Organization**, **WebSite** для главной

### Производительность

- Server-Side Rendering (SSR) для всех страниц
- Кэширование sitemap (revalidate: 3600)
- Оптимизация изображений (next/image)
- Ленивая загрузка компонентов

---

## Шаблон статьи

### Рекомендуемая структура

```markdown
## О чем эта статья

- Пункт 1
- Пункт 2
- Пункт 3

## Проблема / Контекст

Описание проблемы или контекста...

## Решение / Подход

Описание решения...

### Подраздел 1

Детали подраздела...

### Подраздел 2

Детали подраздела...

## Практические примеры

**Промпт:**
\`\`\`
Пример промпта
\`\`\`

**Результат:**
\`\`\`
Пример результата
\`\`\`

## Интеграция с промптами

Ссылки на релевантные промпты на сайте...

## Заключение

Резюме и призыв к действию...
```

### Лучшие практики

1. **Заголовки** - используйте H2 и H3 для структуры
2. **Длина** - оптимально 1500-3000 слов
3. **Примеры** - включайте практические примеры и промпты
4. **Ссылки** - обязательно добавляйте внутренние ссылки
5. **Изображения** - используйте обложку (1200x630px)
6. **Теги** - добавляйте 3-5 релевантных тегов
7. **CTA** - призыв к действию в конце статьи

---

## Добавление нового контента

### Добавление статьи

1. **Через seed скрипт** (рекомендуется для разработки):
   ```bash
   npx tsx scripts/seed-articles.ts
   ```

2. **Через Prisma Studio** (для быстрого редактирования):
   ```bash
   npx prisma studio
   ```

3. **Через админ-панель** (планируется):
   - Будет добавлен визуальный редактор

### Формат контента

- Используйте **Markdown** для форматирования
- Поддерживается **GitHub Flavored Markdown** (GFM):
  - Таблицы
  - Зачеркнутый текст
  - Списки задач
  - Автоссылки

### Добавление категории

```typescript
await prisma.category.create({
  data: {
    slug: 'my-category',
    nameRu: 'Моя категория',
    nameEn: 'My Category',
    descriptionRu: 'Описание на русском',
    descriptionEn: 'Description in English',
    isActive: true,
    sortOrder: 10
  }
});
```

### Добавление тега

```typescript
await prisma.tag.create({
  data: {
    slug: 'my-tag',
    name: 'Мой тег',
    description: 'Описание тега',
    color: '#FF6B6B',
    isActive: true
  }
});
```

---

## Миграции и обновления

### История миграций

- `20251130152559_add_articles` - добавлены модели Article и ArticleTag

### Запуск миграций

```bash
# Создать новую миграцию
npx prisma migrate dev --name your_migration_name

# Применить миграции в продакшн
npx prisma migrate deploy

# Обновить Prisma Client
npx prisma generate
```

---

## Мониторинг и аналитика

### Метрики контента

- Просмотры промптов: `PromptViewEvent`
- Просмотры статей: `Article.viewCount`
- Поисковые запросы: `SearchQuery`
- Популярные теги: агрегация по `PromptTag` и `ArticleTag`

### Отслеживание производительности

- Server-side метрики через OpenTelemetry
- Client-side метрики через Web Vitals
- Логирование через Pino

---

## Деплой и обновление контента

### Деплой на продакшн

```bash
cd /root/prompthub
git fetch origin
git reset --hard origin/main
bash scripts/deploy.sh
```

⚠️ **Важно:** НЕ запускать migrate/seed в проде через скрипт деплоя!

### Обновление контента в проде

1. Подключиться к продакшн БД
2. Запустить seed скрипты вручную или через Prisma Studio
3. Очистить кэш (если используется)

---

## FAQ

### Как добавить новую статью?

Используйте seed скрипт `scripts/seed-articles.ts` как шаблон. Создайте объект статьи с полями на обоих языках, привяжите теги и установите статус `published`.

### Как связать статью с промптами?

Используйте общие теги. Система автоматически найдет связанные промпты по тегам статьи.

### Можно ли добавить HTML в статью?

Нет, используйте только Markdown. React-markdown автоматически преобразует его в безопасный HTML.

### Как управлять переводами?

Для интерфейса используйте файлы в `messages/`. Для контента дублируйте поля с суффиксами `Ru` и `En`.

### Где хранятся изображения?

Пока изображения не хранятся. Планируется интеграция с CDN или S3. Используйте внешние URL.

---

## Roadmap

### Планируется

- [ ] Админ-панель для управления статьями
- [ ] Markdown/WYSIWYG редактор
- [ ] Связь статей с категориями
- [ ] Комментарии к статьям
- [ ] Рейтинги статей
- [ ] Поиск по статьям
- [ ] Фильтры на странице списка статей
- [ ] RSS-фид для статей
- [ ] Экспорт статей в PDF
- [ ] Версионирование статей
- [ ] Соавторство (multiple authors)

---

## Контакты и поддержка

Вопросы и предложения направляйте на team@prompt-hub.site

---

_Документ обновлен: 30 ноября 2025_

