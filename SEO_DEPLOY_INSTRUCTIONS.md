# Инструкции по деплою SEO-оптимизации

## ✅ Код закоммичен и запушен в main

Коммит: `b4f356d` - feat(seo): comprehensive SEO optimization for organic traffic

## 🚀 Команды для деплоя

### Подключение к серверу Orange Curium

```bash
ssh root@SERVER_IP
```

**Пароль**: (из credentials)

### Выполнение деплоя

```bash
cd /root/prompthub && git fetch origin && git reset --hard origin/main && bash scripts/deploy.sh
```

## ⚠️ Важные замечания

1. **НЕ запускать migrate/seed** - согласно правилам, они НЕ должны выполняться в проде
2. Скрипт `scripts/deploy.sh` автоматически:
   - Установит зависимости (npm ci / pnpm install)
   - Сгенерирует Prisma client
   - Соберет приложение (npm run build)
   - Перезапустит через PM2

3. **Проверка после деплоя**:
   ```bash
   # Проверка robots.txt
   curl -i https://prompt-hub.site/robots.txt
   
   # Проверка sitemap
   curl -i https://prompt-hub.site/sitemap.xml
   
   # Проверка SEO-лендингов
   curl -i https://prompt-hub.site/ru/marketpleys-promtov
   curl -i https://prompt-hub.site/ru/baza-promtov
   curl -i https://prompt-hub.site/ru/katalog-promtov
   curl -i https://prompt-hub.site/ru/biblioteka-promtov
   ```

## 📊 Изменения в деплое

Все изменения касаются только кода (TypeScript/TSX файлы), без изменений в БД:
- Новые файлы: SEO-лендинги, route handlers, библиотеки
- Измененные файлы: meta теги, sitemap генерация
- **Миграции БД не требуются**

## 🧪 Проверка работоспособности

После деплоя рекомендуется проверить:
1. Главная страница загружается: https://prompt-hub.site/ru/home
2. Каталог промптов работает: https://prompt-hub.site/ru/prompts
3. robots.txt доступен и содержит Clean-param
4. sitemap.xml валиден и содержит новые SEO-лендинги
5. Все 4 SEO-лендинга доступны и имеют правильные meta теги

